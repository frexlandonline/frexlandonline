import admin from "firebase-admin"; import "dotenv/config"; admin.initializeApp({ credential: admin.credential.cert({ projectId: process.env.FIREBASE_PROJECT_ID, clientEmail: process.env.FIREBASE_CLIENT_EMAIL, privateKey: process.env.FIREBASE_PRIVATE_KEY.replace(/\\n/g, "\n") }) }); const db = admin.firestore(); async function fixDeposit() { const snapshot = await db.collection("users").get(); let found = false; for(const doc of snapshot.docs) { const data = doc.data(); if(data.walletAddresses && data.walletAddresses.map(w => w.toLowerCase()).includes("0x7ca7022c3Ed27534192A2379a5eDd0252b3f6E65".toLowerCase())) { await doc.ref.update({ total_depositado: 10, creditos_escritura: (data.creditos_escritura || 0) + 1 }); found = true; console.log("Updated user " + doc.id); break; } } if(!found) console.log("User not found"); process.exit(0); } fixDeposit().catch(console.error);
