import admin from "firebase-admin"; import "dotenv/config"; admin.initializeApp({ credential: admin.credential.cert({ projectId: process.env.FIREBASE_PROJECT_ID, clientEmail: process.env.FIREBASE_CLIENT_EMAIL, privateKey: process.env.FIREBASE_PRIVATE_KEY.replace(/\\n/g, "\n") }) }); const db = admin.firestore(); async function resetDeposits() { const snapshot = await db.collection("users").get(); const batch = db.batch(); snapshot.docs.forEach(doc => { batch.update(doc.ref, { total_depositado: 0, depositado: 0, balance: 0 }); }); await batch.commit(); console.log("Deposits reset to 0 for " + snapshot.docs.length + " users."); process.exit(0); } resetDeposits().catch(console.error);
